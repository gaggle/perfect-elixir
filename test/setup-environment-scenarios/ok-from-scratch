#!/bin/bash
set -euo pipefail

source "$(dirname "$0")/.init"

precondition "which pkgx" "0"

section "Invoke the script"
expect <<EOF
set timeout 10
spawn /bin/bash -c  $(cmd_for_setup_environment)
expect "Install pkgx?"
send -- "n\r"
expect eof
EOF

#section "Asserts"
#precondition "which pkgx" "0"
